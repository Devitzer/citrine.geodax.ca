---
sidebar_position: 9
---

# bool

The bool keyword is used to declare boolean variables! Will return an error if the variable is not a boolean.

## Example:

```txt title="example.ob"
bool x = true
io.print(x) # output: true
x = "Hello!" # this returns an error
```