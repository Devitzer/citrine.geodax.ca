---
sidebar_position: 8
---

# str

The str keyword is used to declare string variables! Will return an error if the variable is not a string.

## Example:

```txt title="example.ob"
str x = "Hello!"
io.print(x) # output: Hello!
x = 5 # this returns an error
```